[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "gruntmaster"
version = "1.0.0"
requires-python = ">=3.10"

[tool.setuptools]
py-modules = ["run"]

[project.scripts]
run-gruntmaster = "run:main"
